[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "uvicorn newsdiff.main:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/articles"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
